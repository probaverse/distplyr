% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-conditional.R
\name{conditional}
\alias{conditional}
\title{Condition a multivariate distribution on known values}
\usage{
conditional(distribution, given)
}
\arguments{
\item{distribution}{A distribution of several variables.}

\item{given}{The known values, named after their variables: a named
numeric vector, such as \code{c(x = 3)}, or a named list.}
}
\value{
A distribution: univariate if one variable is left over.
}
\description{
The distribution of some of a multivariate distribution's variables,
when the others are known to equal given values.
}
\details{
The result is a distribution of the variables not in \code{given}, in their
original order: univariate if only one is left. It is what the \code{given}
argument of \code{distionary::eval_mv_cdf()} and the like evaluates, but as a
distribution in its own right, to be evaluated, drawn from, or
transformed further.

This conditions on variables \emph{equalling} values. To condition a single
variable on an event, landing above or below a value, see \code{\link[=trim_left]{trim_left()}}
and \code{\link[=trim_right]{trim_right()}}.
}
\section{Slices}{
Conditioning on a variable that is a function of others slices the
distribution. To slice a distribution of \eqn{(X, Y)} along the line
\eqn{X + Y = s}, include \eqn{S = X + Y} as a third variable and
condition on it. The result is still a distribution of \eqn{X} and
\eqn{Y}, but it lives on a line, so its variable type is \code{"singular"}
and it has no density over the plane; take a
\code{distionary::marginal()} of one of them for one that does. For the
multivariate Normal and t this is exact, since the three variables are
again Normal (or t) with a singular covariance.
}

\section{How it is found}{
A distribution can state its own conditionals, as
\code{distionary::dst_mv_norm()} and \code{distionary::dst_mv_t()} do. Otherwise
distionary works one out: for a distribution on finitely many points, by
keeping the points that match \code{given}; for a continuous one, by dividing
densities.

Conditioning on values that cannot occur, or on an \code{NA}, gives the Null
distribution.
}

\examples{
d <- distionary::dst_bi_norm(mean = c(0, 1), sd = c(1, 2), cor = 0.6)
conditional(d, given = c(x = 1))

# Slice two flows along their total.
a <- rbind(r1 = c(1, 0), r2 = c(0, 1), s = c(1, 1))
trio <- distionary::dst_mv_norm(
  mean = stats::setNames(as.numeric(a \%*\% c(50, 80)), rownames(a)),
  cov = a \%*\% matrix(c(100, 60, 60, 225), 2) \%*\% t(a)
)
slice <- conditional(trio, given = c(s = 200))
slice
distionary::marginal(slice, "r1")
}
