% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-smooth_graft.R
\name{smooth_graft_right}
\alias{smooth_graft_right}
\alias{smooth_graft_left}
\title{Smoothly Graft a Tail onto a Distribution}
\usage{
smooth_graft_right(
  distribution,
  tail,
  weight,
  ...,
  threshold = -Inf,
  weight_deriv = NULL
)

smooth_graft_left(
  distribution,
  tail,
  weight,
  ...,
  threshold = Inf,
  weight_deriv = NULL
)
}
\arguments{
\item{distribution}{The body distribution. May be continuous, discrete, or
mixed; in particular it may be an empirical distribution
(\code{\link[distionary:dst_empirical]{distionary::dst_empirical()}}), which a density mixture cannot use.}

\item{tail}{The tail distribution to graft on. Taken continuous (typically a
generalised Pareto distribution, \code{\link[distionary:dst_gp]{distionary::dst_gp()}}, shifted to its
location).}

\item{weight}{The tail weight: a vectorised function on the real line taking
values in \verb{[0, 1]}, giving the share handed to the \code{tail}. For
\code{smooth_graft_right()} it should run from near zero on the body up to near
one in the right tail (non-decreasing); for \code{smooth_graft_left()} it should
run from near one in the left tail down to near zero on the body
(non-increasing). See Details.}

\item{...}{Currently unused; must be empty.}

\item{threshold}{The anchor where the construction starts. Below it (for a
right graft; above it for a left graft) the result is exactly the body; from
it onward the body is handed over to the \code{tail}. Defaults to \code{-Inf}
(\code{smooth_graft_right()}) or \code{Inf} (\code{smooth_graft_left()}), the whole-range
mode in which the \code{weight} alone carries the transition over the whole body.}

\item{weight_deriv}{Optional derivative of \code{weight}, as a vectorised
function. Supplying it avoids a numerical derivative and is more accurate.}
}
\value{
A smooth graft distribution object, a distribution with a continuous
transition between \code{distribution} and \code{tail}.
}
\description{
Replace a distribution's tail with a smooth transition into another
distribution, instead of the hard cut made by \code{\link[=graft_right]{graft_right()}} and
\code{\link[=graft_left]{graft_left()}}. \code{smooth_graft_right()} hands the body over to a right tail;
\code{smooth_graft_left()} hands it over to a left tail. The handover is governed
by a \code{weight} function and follows the hazard-mixture construction of Coia
and De Michele (the "smooth graft"): the body and tail are mixed on the
hazard (right graft) or the reverse hazard (left graft) rather than on the
density, so the result is continuous---no jump or kink in the density, the
hazard, or any risk measure read across the transition---with no normalizing
constant to compute.
}
\details{
Write \eqn{w} for \code{weight} and \eqn{\bar w = 1 - w}. For a right graft, with
body survival \eqn{\bar B}, tail survival \eqn{\bar T}, and densities \eqn{b},
\eqn{t}, the construction mixes the survivals and densities,
\deqn{M(x) = \bar w(x)\bar B(x) + w(x)\bar T(x), \qquad
      m(x) = \bar w(x) b(x) + w(x) t(x),}
and the survival and density are
\deqn{S(x) = M(x)\, C(x), \qquad f(x) = m(x)\, C(x),}
sharing the correction factor
\deqn{C(x) = \exp\left\{-\int_{x_0}^x
       \frac{w'(s)\,[\bar T(s) - \bar B(s)]}{M(s)}\, ds\right\},}
anchored at \eqn{x_0}. At an atom \eqn{a} of the body the graft places mass
\eqn{\bar w(a)\, C(a)\, \mathcal{B}(\{a\})}, where \eqn{\mathcal{B}(\{a\})}
is the body's mass there. The hazard \eqn{h = m / M} carries no correction
factor and is exact and cheap (\code{distionary::eval_hazard()}).

With a finite \code{threshold}, \eqn{x_0} is that threshold: the body and tail are
conditioned to survive to it, the construction runs above it, and the body is
reinstated below (a left graft is the mirror image). With the default
infinite \code{threshold}, \eqn{x_0} is the end of the combined support and the
construction runs over the whole body.

The left graft is the mirror image throughout, built on the CDFs \eqn{F_B},
\eqn{F_T} in place of the survivals.

A constant \code{weight} removes the correction factor (\eqn{C \equiv 1}) and
recovers the static mixture; the transition is what a varying weight buys.

Because the \code{tail} (a GPD) has a density jump at its own location, the graft's
density is smooth there only if \code{weight} is near zero at that location; the
survival, hazard, and risk measures are smooth regardless. This is a property
of the chosen weight, not enforced by the construction.
}
\examples{
library(distionary)
body <- dst_norm(0, 1)
q <- eval_quantile(body, at = 0.9)
tail <- q + dst_gp(scale = 1, shape = 0.3)
# Logistic handover centred above q, so the weight is near zero at the
# tail's location and the density stays smooth there (see Details).
w <- function(x) stats::plogis(x, location = q + 1, scale = 0.4)
g <- smooth_graft_right(body, tail, weight = w)
enframe_cdf(g, at = seq(-2, 5, by = 0.5))

# With a threshold: exactly the body below q, transitioning above it.
g2 <- smooth_graft_right(body, tail, weight = w, threshold = q)
enframe_cdf(g2, at = seq(-2, 5, by = 0.5))
}
